{
  "name": "chatbot",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter chatbot-server start:dev",
    "dev:web": "pnpm --filter chatbot-web dev",
    "build:server": "pnpm --filter chatbot-server build",
    "build:web": "pnpm --filter chatbot-web build",
    "build": "pnpm build:server && pnpm build:web"
  }
}
